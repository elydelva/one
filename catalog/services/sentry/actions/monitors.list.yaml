id: monitors.list
description: List cron monitors for an organization
permission: alerts.read
request:
  method: GET
  path: /organizations/{organization_slug}/monitors/
inputs:
  - name: organization_slug
    type: string
    required: true
    location: path
  - name: cursor
    type: string
    location: query
pagination:
  style: cursor
  request_param: cursor
  request_location: query
  response_token: next_cursor
  max_pages: 10
errors:
  "401": {code: auth_failed}
