version: 1
id: discord
name: Discord
description: Discord HTTP API (v10)
base_url: https://discord.com/api/v10
auth:
  providers:
    - pat
  injection:
    pat:
      header: Authorization
      format: "Bot {access_token}"
