id: checkout.sessions.read
description: Retrieve a Checkout Session.
permission: payments.read
request:
  method: GET
  path: /v1/checkout/sessions/{id}
inputs:
  - name: id
    type: string
    required: true
    location: path
errors:
  "401": {code: auth_failed}
  "404": {code: not_found}
