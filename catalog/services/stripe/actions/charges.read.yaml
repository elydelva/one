id: charges.read
description: Retrieve a charge.
permission: payments.read
request:
  method: GET
  path: /v1/charges/{id}
inputs:
  - name: id
    type: string
    required: true
    location: path
errors:
  "401": {code: auth_failed}
  "404": {code: not_found}
