id: filters.list
description: Get the user's favourite filters
permission: issues.read
side_effects: read
request:
  method: GET
  path: /rest/api/3/filter/favourite
inputs:
  - name: expand
    type: string
    location: query
errors:
  "401": {code: auth_failed}
