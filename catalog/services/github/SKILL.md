# GitHub

Use the GitHub REST API via `one github <action>`. Authentication: PAT (token paste) or OAuth user-flow.

Common actions:
- `issues.list` — list issues for a repo
- `issues.read` — read one issue
